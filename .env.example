# Base URL for the CoinGecko API. Optional — src/lib/coins.ts falls back to
# the public endpoint below if this is unset. Point it at a paid/proxied
# CoinGecko instance to avoid the public tier's rate limit.
VITE_COINGECKO_API_BASE=https://api.coingecko.com/api/v3
